# Access Control List for MQTT broker
# Format: topic [read|write|readwrite] <topic>

# Allow sensors to publish to their own telemetry topics
pattern write greenhouse/%c/telemetry

# Allow fog service to subscribe to all sensor telemetry
user fog
topic read greenhouse/+/telemetry

# Allow admin to access everything
user admin
pattern readwrite #

# Default: deny all
topic deny #